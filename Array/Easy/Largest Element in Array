class Solution {
  public:
    int largest(vector<int> &arr) {
        int i=0;
        int biggest=arr[0];
        for(int i=1;i<arr.size();i++){
            if(arr[i]>biggest){
                biggest=arr[i];
            }
        }
        return biggest;
    }
};
